# Netflix Viewing History Data Viz

1.) Run `npm install`
2.) `git clone git@github.com:shail/netflix_viewing_api.git`
  - `bundle install`
  - `ruby server.rb`
3.) Run `npm run start`
4.) Navigate to `localhost:8080/`
